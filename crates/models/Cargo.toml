[package]
name = "codeframe-models"
description = "Shared vocabulary types for CodeFrame (no platform dependencies beyond serde)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
